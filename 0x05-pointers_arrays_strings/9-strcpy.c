#include "holberton.h"

/**
 * *_strcpy - string
 * @src: char
 * @dest: char
 * Return: return.
 */

char *_strcpy(char *dest, char *src)
{
int a;
int b;

for (b = 0; src[b]; b++)
{
}
for (a = 0; a <= b; a++)
{
dest[a] = src[a];
}
return (dest);
}
